// Generated by CoffeeScript 1.6.3
define(function(require, exports, module) {
  var datasource, server, sinon;
  require("sinon");
  sinon = window.sinon;
  datasource = require("./datasource");
  server = sinon.sandbox.useFakeServer();
  return module.exports = function(url) {
    server.respondWith(url, function(xhr) {
      var request;
      request = xhr.requestBody;
      if (request === null || request === void 0) {
        request = "";
      }
      return xhr.respond(200, {
        "Content-Type": "application/json"
      }, JSON.stringify({
        err: null,
        data: datasource[url][request]
      }));
    });
    return server.respond();
  };
});
